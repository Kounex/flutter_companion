import 'package:basic_layout/app.dart';
import 'package:flutter/material.dart';

/// The classic - the main. Also in Flutter we need a starting point. You could argue to
/// put it in the app.dart file and not seperate it. There are cases where you need to
/// do stuff even before actually running the app so it is just cleaner seperating it
/// generally.
void main() => runApp(App());
